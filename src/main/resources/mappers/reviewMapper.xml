<?xml version="1.0" encoding="UTF-8"?>

<!-- dtd : xml에서 사용된 태그를 정의한 문서 
	 mybatis에 dtd 기반으로 태그의 규칙이 적용이 됐다
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <!-- 해당 하는 repository와 연결 -->
 <mapper namespace="com.jinju.spring.ex.mybatis.repository.ReviewRepository">
 	<select id="selectReview" 
 	resultType="com.jinju.spring.ex.mybatis.domain.Review"
 	parameterType="int"><!-- 파라미터 추가 됐으니 parameterType="int" 추가 -->
 		SELECT
 			`id`
 			, `storeId`
 			, `menu`
 			, `userName`
 			, `point`
 			, `review`
 			, `createdAt`
 			, `updatedAt`
 		FROM 
 			`new_review`
 		WHERE
 			`id` = #{id}
 			
 	</select>
 	
 	<!-- 
 	mybatis dtd에서 insert 태그에는 resultType 속성을 정의해 놓지 않음
 	insert / delete / update resultType은 속성 자체를 지정 할 필요 없이 고정된 int 라는 리턴 값이 만들어짐 -->
 	<insert id="insertReview" parameterType="map">
 		INSERT INTO `new_review`
 		(
 			`storeId`
 			, `menu`
 			, `userName`
 			, `point`
 			, `review`		
 		)
 		VALUE
 		(
 			#{storeId}
 			, #{menu}
 			, #{userName}
 			, #{point}
 			, #{review}
 		)
 	</insert>
 	
 	<insert id="insertReviewByObject" 
 	parameterType="com.jinju.spring.ex.mybatis.domain.Review">
 		INSERT INTO `new_review`
 		(
 			`storeId`
 			, `menu`
 			, `userName`
 			, `point`
 			, `review`		
 		)
 		VALUE
 		(
 			#{storeId}
 			,#{menu}
 			,#{userName}
 			,#{point}
 			,#{review}
 		)
 	</insert>
 </mapper>